# HMCTS Copy resource group expireAfter tag

This policy will copy the expireAfter tag if it exist on the resource group and add it to a newly created resource.  It will only run on sandbox environments.