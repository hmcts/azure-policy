# HMCTS Copy resource group expiresAfter tag

This policy will copy the expiresAfter tag if it exist on the resource group and add it to a newly created resource.  It will only run on sandbox environments.
